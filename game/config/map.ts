/**
 * V30 : les paramètres cartographiques ne sont plus exportés comme constantes
 * Île-de-France globales. Les consommateurs doivent demander la définition du
 * territoire de la sauvegarde via config/territories.ts.
 */
export { getGameTerritoryMapDefinition } from './territories'
